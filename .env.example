# Copy to .env and fill. Never commit .env.

# Supabase — subscribers, rain reports, message + broadcast log. Schema: schema/supabase.sql
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=
# Same project, VITE_-prefixed so the web bundle can read them at build time (Vite only
# exposes VITE_* to client code). Anon key only — never put SUPABASE_SERVICE_KEY here;
# RLS in schema/supabase.sql is what actually limits what the anon key can touch.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Telegram — primary demo channel (@BotFather)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# WhatsApp Cloud API — test number, 5 verified recipients
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_TEST_RECIPIENT=
# Only needed if a webhook receiver gets built later (Meta calls back with this
# token to verify the endpoint) — nothing in this project reads it yet.
WHATSAPP_VERIFY_TOKEN=

# Indic Parler-TTS — local audio server (no API key needed, runs offline)
# Start with: .venv/bin/python services/tts/server.py
# The PWA auto-detects if the server is running and shows an HD badge.
TTS_SERVER_URL=http://localhost:8765

# Officer dashboard's "Review broadcast" button, via services/broadcast_server.py
# (localhost:8787 — the frontend has no real backend yet). Pick any long random
# string; the officer's browser sends it back with every broadcast request, and a
# request without the matching token is rejected. Without this set, broadcasting
# from the dashboard is refused entirely, on purpose.
OFFICER_BROADCAST_TOKEN=

# Pipeline
PILOT_STATE=karnataka
